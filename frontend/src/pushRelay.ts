/**
 * Frappe push relay project id.
 * Must match `EXCOM_RELAY_PROJECT_NAME` in `excom/excom/notification.py` and relay / FC registration.
 */
export const EXCOM_RELAY_PROJECT_NAME = "excom" as const;
